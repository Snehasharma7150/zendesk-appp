[build]
builder = "nixpacks"

[deploy]
startCommand = "node src/server.js"
restartPolicyType = "always"
restartPolicyMaxRetries = 3

[deploy.healthcheck]
path = "/api/health"
intervalSeconds = 30
timeoutSeconds = 10
